---
  - name: Main
    hosts: "story-nodes"
    gather_facts: false
    vars_files:
      - vars/story.yaml
    roles:
      - story_install
      - geth_install
      - node_initialize
      - node_configure
      - node_launch
